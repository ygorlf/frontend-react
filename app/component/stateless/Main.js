import React from 'react';
import Join from './join/Join';
import FeatureList from './join/FeatureList';

const Main = props => (
  <div>
  	<Join/>
		<FeatureList/>
  </div>
);

export default Main;
